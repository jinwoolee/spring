<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<!-- http : security web 보안 설정 
	
		 스프링 컨테이너가 springSecurityFilterChain 이라는 이름으로 스프링 빈을 등록
		 intercept-url : pattern에 일치하는 url 요청에 대해 보안 처리	 
	
			기본 로그인 화면 url(스프링에서 화면 제공) : /login (spring security 버전마다 url이 상이) 
		 	
		 	/main url로 요청시 ROLE_ADMIN 권한을 획득한 사람만 접속 가능
		 	
		 	auto-config (default:false) : HTTP 기본인증, 폼 로그인 인증, 로그아웃 설정 
		 	use-expressions (default:true) : access 속성에서 SPEl 사용여부
		 	
		 form-login : 커스텀 로그인 페이지 설정
		 logout : 로그아웃 동작 설정
		 	logout-url : 로그아웃 처리 url
		 	logout-success-url : 로그아웃 정상 처리후 이동할 url
		 	invalidate-session (default:true) : 로그아웃 처리후 세션 제거 여부
		 	
		 
		 intercept-url : 검증할 url과 인가 정보 설정
		                 위에서 아래로 적용됨 ==> 구체적인것 -> 추상적인것  									 			 	 	
	-->
	
	<security:http auto-config="true" use-expressions="true" >
		<security:form-login/>
		<security:logout />
		<security:intercept-url pattern="/login" access="hasAnyRole('ROLE_ADMIN', 'ROLE_USER')" />
		<security:intercept-url pattern="/main" access="hasRole('ROLE_ADMIN')" />
	</security:http>
	
	
	<!-- authentication-manager : 인증 요청 처리
		 authentication-provider : 인증 정보를 레파지토리(인메모리, db, LDAP)로 부터 조회
		    					   설정된 UserDetailService(interface)를 사용
		 user-service : 인메모리 방식의 UserDetailService(interface) 설정 -->
	<security:authentication-manager>
		<security:authentication-provider>
			<security:user-service>
				<security:user name="brown" password="12345" authorities="ROLE_ADMIN"/>
			</security:user-service>
		</security:authentication-provider>
	</security:authentication-manager>

</beans>
