PlaceHolderTest 테스트 코드 생성
user, password, driver, url 각 속성이
db.properties에 설정한 값과 일치하는지 테스트


lotto 요구사항을
1.사용자로부터 공의 갯수를 입력받을 수 있다
   (한국 로또의 경우 45, 미국의 경우는 55)

2.추첨 공의 갯수도 사용자로부터 입력 받을 수 있다

3.실행시 추첨 공의 갯수만큼의 정수를 담은 배열을
  리턴 해준다.

4.추첨된 공은 중복 될수 없다.

5.실행할 수 있는 테스트코드를 같이 만든다  

6.package : kr.or.ddit.lotto

@Test
public void lottoTest(){
	
	Lotto lotto = new Lotto();
	lotto.setMaxNum(55);
	lotto.setBallNum(6);
	int[] lotts = lotto.execute();
}  


  
  
  
