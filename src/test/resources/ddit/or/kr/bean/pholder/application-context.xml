<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd">
	
	<!-- src/test/resources -->
	<context:property-placeholder 
		location="classpath:/ddit/or/kr/bean/property/db.properties"
		system-properties-mode="NEVER"/>
		
	<!-- username : 해당 피씨의 사용자 이름 keyword -->
	
	<!-- <bean id="placeHolder" class="ddit.or.kr.bean.pholder.PlaceHolder">
		<property name="driver" value="${jdbc.driver}"/>
		<property name="url" value="${jdbc.url}"/>
		<property name="dbuser" value="${jdbc.dbuser}"/>
		<property name="password" value="${jdbc.password}"/>
	</bean> -->
	
	<bean id="placeHolder" class="ddit.or.kr.bean.pholder.PlaceHolder">
		<property name="driver" value="${driver}"/>
		<property name="url" value="${url}"/>
		<property name="dbuser" value="${username}"/>
		<property name="password" value="${password}"/>
	</bean>

</beans>
